app = "vexr-bot"
kill_signal = "SIGINT"
kill_timeout = 5
processes = []

[build]
  builder = "python"

[[services]]
  internal_port = 8080
  protocol = "tcp"
  auto_start_machines = true
  auto_stop_machines = false

  [[services.ports]]
    port = 443
    handlers = ["http", "tls"]
